_schema-version: 3.3.0
ID: CAP-native
description: A simple CAP project.
version: 1.0.0
modules:
- name: CAP-native-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: CAP-native-auth
  - name: CAP-native-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: CAP-native-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: CAP-native-db
  parameters:
    buildpack: nodejs_buildpack
- name: CAP-native-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: CAP-native-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - fioriapp01.zip
      name: fioriapp01
      target-path: resources/
- name: fioriapp01
  type: html5
  path: app/fioriapp01
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: CAP-native-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: CAP-native-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: CAP-native-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: CAP-native-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: CAP-native-html5-srv
    service-plan: app-host
- name: CAP-native-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: CAP-native-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
